1. RestControllerAdvice
- @RestControllerAdvice는 Spring Framework에서 예외를 전역적으로 처리하기 위한 어노테이션입니다. 주로 모든 @RestController에서 발생하는 예외를 한 곳에서 처리하기 위해 사용됩니다.

  장점: 예외를 일관되게 처리하고, 개별 컨트롤러마다 예외 처리를 작성할 필요가 없어 코드 중복이 줄어듭니다.
  사용법: @ExceptionHandler를 사용해 특정 예외에 대한 응답을 정의할 수 있습니다.
  응답의 일관성: 전역적으로 처리되므로 클라이언트는 항상 일관된 형식으로 에러 응답을 받을 수 있습니다.

2. lombok
- Lombok은 Java 코드에서 반복적인 코드를 자동으로 생성해 주는 라이브러리입니다. 주로 Getter, Setter, 생성자, toString, equals 등의 메서드를 어노테이션으로 간단하게 추가할 수 있어 코드를 간결하게 만들어줍니다.

  주요 어노테이션:
  @Getter / @Setter: 필드의 Getter와 Setter 메서드를 자동 생성합니다.
  @ToString: 객체의 toString 메서드를 자동 생성합니다.
  @EqualsAndHashCode: equals와 hashCode 메서드를 자동 생성합니다.
  @NoArgsConstructor / @AllArgsConstructor / @Builder: 다양한 생성자와 빌더 패턴을 쉽게 생성할 수 있습니다.
  장점: 보일러플레이트 코드를 줄여서 코드 가독성과 유지보수성을 높입니다.

  주의점: IDE에 Lombok 플러그인이 설치되어 있어야 어노테이션이 적용된 코드를 컴파일할 수 있습니다.

